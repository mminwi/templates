# {{Project name}} — Component Library

Inventory of every reusable UI component (e.g., under `src/components/`). Page-co-located client components live next to their `page.tsx` and are listed at the end without full blocks.

**Field reference:**
- **Purpose** — one-line reason the component exists
- **Path** — file path
- **Type** — Server | Client | both
- **Props** — key props with types (3-6 most important)
- **Used by** — pages or higher-level components that consume it
- **Notes** — accessibility, theme variant, edge cases

---

<!-- TODO: replace this skeleton with one block per reusable component.

## Layout

### ComponentName
- **Purpose:** ...
- **Path:** src/components/layout/...
- **Type:** Client
- **Props:** ...
- **Used by:** src/app/layout.tsx
- **Notes:** ...

-->

## Page-co-located client components

<!-- TODO: list every `.tsx` file (not `page.tsx`) under your role/area folders, one bullet per file with a one-line description. -->
